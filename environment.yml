name: mdfptools
channels:
    - omnia
    - rdkit
dependencies:
    # conda build dependencies
    - rdkit
    - openforcefield
    - mdtraj
    - pip:
      - nbsphinx_link
      - recommonmark
      - nbsphinx
      - numpydoc
